.travis.yml:
  delete: true
appveyor.yml:
  delete: true

.gitlab-ci.yml:
  unmanaged: true

Gemfile:
  required:
    ':development':
      - gem: 'beaker-rspec'
      - gem: 'beaker-puppet'
      - gem: 'beaker-docker'
      - gem: 'beaker-puppet_install_helper'
      - gem: 'beaker-module_install_helper'
